machine:
  ghc:
    version: 8.0.1 

dependencies:
  pre:
    - mkdir -p ~/.cache || true
    - mkdir -p ~/.stack || true

  override:
    - stack setup
    - stack build

test:
  override:
    - stack test
