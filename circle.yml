machine:
  ghc:
    version: 7.10.3

dependencies:
  pre:
    - mkdir -p ~/.cache || true
    - mkdir -p ~/.stack || true

  override:
    - stack setup
    - stack build

test:
  override:
    - stack test
